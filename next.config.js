/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "developerjones.dk", "dev.gamingfriends.gg", "medium.com"]
  }
}

module.exports = nextConfig
